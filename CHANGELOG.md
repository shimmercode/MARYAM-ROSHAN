# تاریخچه تغییرات

قالب این فایل بر پایه [Keep a Changelog](https://keepachangelog.com/fa/1.1.0/) است و
نسخه‌گذاری از [Semantic Versioning](https://semver.org/lang/fa/) پیروی می‌کند.

## [0.1.6] — 1404/06/22 (2026-09-13)

بازسازی دقیق پنل مدیریت برای تطابق **پیکسلی** با طرح HTML تأییدشده مشتری —
نه فقط «الهام‌گرفته از»، بلکه واژگان کلاس CSS طرح («.kc»، «.bs»، «.ns/.ni»،
«.header»، «.invoice-preview» و…) عیناً در استایلی جدید (`mockup.css`) اضافه
شد و در کامپوننت‌های اشتراکی و صفحات کلیدی به‌کار رفت. هیچ داده ساختگی یا
منطق سمت کلاینت اضافه نشد؛ همهٔ اعداد از کوئری واقعی دیتابیس می‌آیند.

### تغییر کرد — منوی کناری (درخواست اصلی مشتری)

- نوار کناری کاملاً بازسازی شد تا **دقیقاً** ساختار منوی تأییدشده مشتری را
  با ۶ گروه («اصلی»، «فروش و مالی»، «CRM و وفاداری»، «بازاریابی»، «انبار و
  تأمین»، «Enterprise»)، نقطهٔ رنگی هر گروه و ۴۹ آیتم منو پیاده کند.
- هر عنوان گروه (`.ns-t`) اکنون **کلیک‌پذیر و آکاردئونی** است: کلیک روی
  عنوان، لیست آیتم‌های همان گروه را باز/بسته می‌کند (جاوااسکریپت خالص، بدون
  فریم‌ورک جدید)؛ گروه مربوط به صفحهٔ جاری به‌صورت پیش‌فرض باز است. این دقیقاً
  همان رفتاری است که مشتری درخواست کرده بود («منو باید ساب‌منوی زیرش
  دراپ‌داون باز شه»).
- نشان‌های عددی کنار «نوبت‌ها» و «فاکتورها» از کوئری واقعی (تعداد نوبت‌های
  امروز، تعداد فاکتورهای پرداخت‌نشده/جزئی) پر می‌شوند، نه عدد ثابت طرح.
- برای آیتم‌های منو که در طرح تأییدشده مفهومی واقعی‌اند اما هنوز پیاده‌سازی
  سرور ندارند، به‌جای صفحهٔ ساختگی، کنترلر/ویوی سبک جدید
  `Admin\ComingSoonController` («این بخش به‌زودی فعال خواهد شد») اضافه شد تا
  آیتم منو کلیک‌پذیر باشد و ۴۰۴ ندهد؛ نشان «به‌زودی» روی این آیتم‌ها دیده
  می‌شود. فهرست کامل نگاشت منو → مسیر واقعی/به‌زودی در گزارش تحویل آمده است.

### افزوده شد

- `public/assets/css/mockup.css`: لایهٔ جدید وفاداری بصری — واژگان کلاس طرح
  به‌صورت عینی (برای سایدبار/هدر/KPI/نشان وضعیت/فرم ورود/پیش‌نمایش
  فاکتور/سبد POS) به همراه رنگ‌ها از طریق alias به توکن‌های موجود `--mr-*`
  (بدون افزودن منبع رنگ موازی)؛ و تنظیم مقادیر (پدینگ/شعاع/سایه) کلاس‌های
  اشتراکی موجود (`.mr-card`, `.mr-btn`, مودال بوت‌استرپ, `.mr-tabs`, …) تا
  خروجی رندرشده با طرح یکسان باشد حتی وقتی نام کلاس عوض نشده.
- `App\Controllers\Admin\ComingSoonController` + `admin/coming_soon.php`.
- `BaseController::sidebarBadges()`: دو شمارندهٔ واقعی (نوبت‌های امروز،
  فاکتورهای معوق) برای نشان‌های سایدبار، با fail-soft به صفر در صورت خطا.

### تغییر کرد — کامپوننت‌ها و صفحات

- `components/kpi.php` و `components/status_badge.php` به واژگان دقیق طرح
  (`.kc/.kt/.ki/.kv/.kl/.kch/.ks` و `.bs` + کلاس وضعیت) بازنویسی شدند —
  چون این دو کامپوننت در ده‌ها صفحه استفاده می‌شوند (داشبورد، پرونده مشتری،
  پرونده پرسنل، فاکتورها)، این تغییر بدون دست‌زدن به هر صفحه اعمال شد.
  نگاشت وضعیت‌های واقعی دیتابیس (نوبت‌ها، `payment_status`/`status` فاکتور،
  `status` پرداخت) به کلاس‌های دقیق طرح (`confirmed/pending/completed/paid/
  active/ok/sent/success/cancelled/low/danger/vip/inactive/closed/new/open`)
  به‌صورت صریح انجام شد.
- هدر پنل مدیریت (`partials/admin/topbar.php`) با واژگان `.header/.h-r/.h-l/
  .pt/.sb/.ib/.ub/.av/.ui` بازسازی شد (جستجو، زنگ اعلان با نشان، آواتار+نام+
  نقش).
- فاکتور: پارشیال مشترک جدید `partials/admin/invoice_preview.php` با واژگان
  دقیق `.invoice-preview/.inv-header/.inv-brand/.inv-meta/.inv-body/
  .inv-info-grid/.inv-table/.inv-totals/.inv-footer/.inv-barcode/.inv-thanks`
  ساخته شد و هم در صفحهٔ نمایش فاکتور (`admin/invoices/show.php`) و هم در
  چاپ (`admin/invoices/print.php`) استفاده می‌شود تا این دو هرگز واگرا
  نشوند؛ داده‌ها همگی واقعی (اقلام، مشتری، شعبه، وضعیت پرداخت).
- پرونده پرسنل: سربرگ به `.prh/.prav/.pri` و آمار سریع به کاشی‌های
  `.pk/.pki/.pkv/.pkl` تبدیل شد.
- صندوق فروش: ردیف‌های سبد کلاس `.pos-ci` گرفتند (در کنار `.slot-row`
  موجود).
- فرم ورود: کانتینرها و فیلدها با کلاس‌های `.login-page/.login-box/
  .login-form/.fg/.fc/.btn-login` علامت‌گذاری شدند (منطق ورود واقعی دست‌
  نخورده ماند؛ بدون راهنمای اطلاعات ورود آزمایشی).

### نگاشت نهایی منو → مسیر واقعی یا صفحهٔ «به‌زودی»

لینک واقعی: داشبورد، نوبت‌ها، تقویم، خدمات، پرسنل، شیفت‌بندی(→پرسنل)، شعب،
مشتریان، POS، فروش(گزارش)، فاکتورها، Customer 360(→مشتریان)، باشگاه
مشتریان(→تخفیف‌ها)، کمپین‌ها، Marketing Automation، تخفیف‌ها، محصولات، انبار،
گردش کالا، BI & Analytics(→گزارش‌ها)، Audit Log، کاربران، اعلان‌ها، تنظیمات،
پشتیبان‌گیری، چندشعبه‌ای(→شعب)، وب‌سایت عمومی (لینک خارجی
maryamroshan.com)، پنل مشتری، پنل پرسنل.

صفحهٔ «به‌زودی» (بدون داده ساختگی): موتور رزرو، منابع و ظرفیت، پرداخت‌ها،
کیف پول، پورسانت، بهای تمام‌شده، سودآوری، مدیریت ریزش، نظرسنجی‌ها،
Reputation، تیکت و شکایات، ارتباطات (Omnichannel)، Waitlist،
تأمین‌کنندگان، درخواست خرید، Workflow، Security، نقش‌ها، Templateها،
Franchise.

### ارتقا

نسخه برنامه (`config/app.php` → `version`) از `0.1.5` به `0.1.6` ارتقا یافت.
تغییر پایگاه‌داده‌ای در این نسخه وجود ندارد.

### روش تأیید

با اجرای واقعی روی PHP 8.4 + MariaDB (schema.sql + seeds.sql روی دیتابیس
تازه) و Playwright/Chromium: اسکرین‌شات از ورود، داشبورد (سایدبار کامل باز)،
یک گروه بسته‌شده با کلیک (تأیید آکاردئون)، پرونده پرسنل با تب‌ها، لیست و
نمایش/چاپ فاکتور، POS؛ و کلیک واقعی روی همهٔ ۴۹ آیتم منو (به‌جز لینک‌های
خارجی/پورتال که در تب جدید باز می‌شوند) بدون هیچ خطای PHP یا صفحهٔ ۴۰۴.

## [0.1.5] — 1404/06/22 (2026-09-13)

بازطراحی بصری پنل مدیریت برای هم‌راستایی با طرح تأیید‌شده — بدون هیچ تغییری در
منطق سرور یا داده‌های واقعی؛ فقط CSS و ساختار Viewها. توکن‌های رنگ (`tokens.css`)
از قبل با طرح تأیید‌شده یکسان بودند و دست نخوردند.

### تغییر کرد

- **ورود (`/login`)**: چیدمان دو‌ستونه با پنل برند در کنار فرم — ویژگی‌های کلیدی
  محصول (مدیریت نوبت‌ها، پرونده ۳۶۰ مشتری، گزارش لحظه‌ای) و سه شمارنده آماری
  **واقعی** (تعداد مشتریان، متخصصان و نوبت‌های ثبت‌شده از دیتابیس، نه عدد ساختگی).
  در صفحات نصاب/خطا (که همان لایوت را به‌اشتراک می‌گذارند) بدون تغییر باقی ماند.
- **کارت‌های KPI** (`component('kpi')`): آیکن داخل دایرهٔ رنگی هم‌راستا با نوع
  شاخص (موفق/هشدار/خطر/اطلاعاتی) اضافه شد؛ در داشبورد، پرونده مشتری، پرونده
  پرسنل و لیست فاکتورها به‌کار رفت.
- **نوار کناری**: گروه‌بندی مجدد آیتم‌های واقعی منو به‌شکل «اصلی»، «فروش و مالی»،
  «CRM و وفاداری» (مشتریان + باشگاه وفاداری/تخفیف)، «بازاریابی»، «انبار و تأمین»
  و «Enterprise» (اعلان‌ها/تنظیمات)؛ نقطهٔ رنگی هر گروه حفظ شد و نشان تعداد
  اعلان نخوانده (از `NotificationService::unreadCount`) روی آیتم «اعلان‌ها» اضافه شد.
- **پرونده مشتری و پرونده پرسنل**: سربرگ به الگوی آواتار + نام + تراشه‌های آمار
  سریع (خرید/مراجعه/امتیاز برای مشتری؛ درآمد/نوبت/امتیاز برای پرسنل) تبدیل شد.
- **جدول‌های فاکتورها، نوبت‌ها و مشتریان**: ستون عملیات با دکمه‌های آیکنی
  (مشاهده/ویرایش/چاپ) به‌جای دکمه‌های متنی.
- **چاپ فاکتور**: هدر و جدول اقلام با رنگ برند (نویی) و نشان سالن، به‌جای پالت
  خنثی قبلی.
- **صندوق فروش**: کلاس‌های `pos-layout`/`pos-cart` و استایل‌های `.pick`/`.slot-row`
  که پیش‌تر فقط در CSS سایت/پرتال بودند (و در پنل مدیریت بارگذاری نمی‌شدند) به
  `app.css` منتقل شدند تا چیدمان دوستونهٔ انتخاب کالا/سبد چسبان درست نمایش داده شود.

## [0.1.4] — 1404/06/22 (2026-09-13)

دو قابلیت جدید روی پنل مدیریت افزوده شد: بخش «آیین‌نامه و اسناد» در تنظیمات، و
چهار فرم زنده و قابل‌ذخیره در دیتابیس داخل پرونده هر پرسنل. سایت عمومی، نصاب و
هرچه در نسخه‌های ۰.۱.۲/۰.۱.۳ رفع شده بود دست‌نخورده باقی ماندند.

### افزوده شد

- **تنظیمات › آیین‌نامه و اسناد** (`/admin/settings/documents`): بخش کاملاً جدید
  برای بارگذاری، فهرست، دانلود و حذف اسناد سالن (آیین‌نامه، قرارداد، صورتجلسه،
  بازاریابی، سایر). فایل‌ها **خارج از مسیر عمومی** در `storage/documents/` با نام
  تصادفی ذخیره می‌شوند و فقط از طریق کنترلر (با بررسی نقش/دسترسی مدیر) قابل دانلودند
  — دقیقاً همان الگوی امنیتی که پشتیبان‌گیری پایگاه‌داده از قبل استفاده می‌کرد.
  جدول جدید `documents` (عنوان، توضیحات، دسته‌بندی، نام اصلی/ذخیره‌شده فایل، نوع
  MIME، حجم، بارگذاری‌کننده). فرمت‌های مجاز بارگذاری (`config/app.php` → `uploads`)
  با افزودن **docx** و **pptx** (و MIMEهای Office Open XML متناظر) گسترش یافت.
  چهار سند واقعی سالن به‌عنوان محتوای اولیه بارگذاری شدند: «بازاریابی مدرن در سالن
  زیبایی مریم روشن - رویکرد سفر مشتری» (بازاریابی)، «قرارداد مشارکت در منافع صندلی -
  آذر ۱۴۰۴» (قرارداد)، «آیین‌نامه - آذر ۱۴۰۴» (آیین‌نامه) و «صورتجلسه ارزیابی
  عملکرد پرسنل (نسخه خام)» (صورتجلسه).

- **چهار تب فرم زنده در پرونده پرسنل** (`/admin/staff/{id}`، مشابه دقیق الگوی تب‌های
  پرونده مشتری): هر فرم مستقیماً در دیتابیس ثبت و بلافاصله در همان صفحه لیست می‌شود.
  - **آفر در لحظه** (جدول `instant_offers`): دقیقاً ستون‌های کاربرگ اصلی («فرم آفر
    در لحظه») — نام/نام‌خانوادگی/تماس مشتری (یا اتصال به پرونده مشتری موجود با کد
    مشتری)، لاین خدمات، نحوه آشنایی، سه لاین پیشنهادی، تاریخ رزرو/خرید/خرید بعدی،
    توضیحات پشت برگه.
  - **گزارش سفر مشتری** (جدول `customer_journey_entries`): بر پایه نسخه ۲ کاربرگ
    «گزارش سفر مشتریان پرسنل» (بدون فیلد تلفن، چون شماره تماس از قبل در پرونده
    مشتری موجود است) — مشاوره/نوبت‌دهی/خرید/رضایت/خرید بعدی/ارجاع به مدیر داخلی.
  - **جدول مدیریت مشتریان** (جدول `customer_management_reports`): بازسازی کاربرگ
    «جدول مدیریت مشتریان» به‌صورت گزارش دوره‌ای مقایسه‌ای (دوره گزارش / دوره قبل) به
    ازای هر لاین خدمات، با همان ۱۱ شاخص کاربرگ اصلی (تعداد مشتریان، مشتریان جدید،
    بازگشت، درصد وفادارسازی، مبلغ/تعداد فروش، اعتبار سالن، آفر، سایر کمپین‌ها، رتبه).
  - **ارزیابی عملکرد پرسنل** — جدول موجود `performance_evaluations` **بسط داده شد**
    (نه بازسازی موازی): دو ستون جدید `special_score` و `rank_label`، و جدول فرزند
    جدید `performance_evaluation_items` برای ۱۶ ردیف شاخص‌محور رابریک رسمی.
    سرویس جدید `App\Services\PerformanceEvaluationService` الگوریتم امتیازدهی را
    عیناً از روی «فرم ارزیابی-نسخه۶.۱۴۰۴» و «جدول امتیاز بندی و رتبه بندی» پیاده
    کرده (نسخه قدیمی‌تر کاربرگ که در همان فایل بود عمداً نادیده گرفته شد):
    - هر شاخص امتیاز خام ۰ تا ۳ می‌گیرد (ضعیف=۰، متوسط=۱، خوب=۲، عالی=۳) و
      امتیاز موزون آن = امتیاز خام × ضریب شاخص.
    - مجموع ۱۶ امتیاز موزون (حداکثر ۹۶) + حداکثر ۴ «امتیاز ویژه از دیدگاه مدیریت»
      = نتیجه عملکرد نهایی از ۱۰۰.
    - رتبه‌بندی دقیقاً طبق جدول اصلی: ۸۰–۱۰۰ «ممتاز» (بسیار اثربخش)، ۷۰–۷۹ «عالی»
      (اثربخش)، ۶۰–۶۹ «خوب» (مطلوب)، ۵۰–۵۹ «متوسط» (نیاز به عارضه‌یابی)، کمتر از
      ۵۰ «ضعیف» (نامطلوب) — هرکدام با متن دقیق سیاست پاداش/تذکر/جریمه/قطع همکاری
      کاربرگ اصلی، که در همان صفحه به مدیر نمایش داده می‌شود.

### تغییر کرد

- `config/app.php`: افزودن `docx`/`pptx` و MIMEهای متناظر به فهرست بارگذاری مجاز
  (و افزودن `application/zip` به‌عنوان بازگشت رایج fileinfo برای فایل‌های
  Office Open XML روی برخی سرورها).
- نسخه برنامه (`config/app.php` → `version`) از `0.1.3` به `0.1.4` ارتقا یافت.

### ارتقای نصب‌های موجود (production)

نصب‌های قبل از این نسخه باید `database/upgrade_0.1.4.sql` را یک‌بار (مثلاً از طریق
phpMyAdmin) اجرا کنند — این اسکریپت idempotent است. جزئیات کامل در بخش «ارتقا از
نسخه قبلی» در `README.md`.

### روش تأیید

تمام موارد بالا با اجرای واقعی پروژه (PHP 8.4 + MariaDB 10.11 واقعی، نه فقط
`php -l`) آزمایش شدند: `database/schema.sql` و `database/upgrade_0.1.4.sql` هر دو
از صفر و به‌صورت idempotent روی همان پایگاه‌داده اجرا شدند؛ ورود با حساب مدیر واقعی؛
بارگذاری هر چهار سند واقعی و دانلود موفق آن‌ها (با تطابق دقیق حجم فایل و نوع MIME)؛
ثبت واقعی یک ردیف در هر یک از چهار فرم پرونده پرسنل از طریق POST و بازبینی فوری آن
در همان صفحه؛ محاسبه صحیح الگوریتم ارزیابی عملکرد (آزمایش با امتیاز ۲ در همه ۱۶
شاخص + امتیاز ویژه ۳ → نتیجه ۶۷.۰، دقیقاً برابر ۲×۳۲+۳). مجموعه تست واحد پروژه
(۵۹ بررسی) و بررسی بیش از ۱۵ مسیر پنل مدیریت بدون خطای PHP اجرا شدند.

## [0.1.3] — 1404/06/22 (2026-09-13)

رفع یک باگ مهم که باعث می‌شد پیام‌های خطای اعتبارسنجی (مثلاً «شماره موبایل نامعتبر است»
یا «تکرار رمز عبور مطابقت ندارد») در **هیچ‌کدام از صفحات برنامه** — از جمله نصاب —
روی صفحه نمایش داده نشوند. نتیجهٔ این باگ دقیقاً همان چیزی بود که گزارش شد: کاربر
فرم را پر می‌کرد، دکمه را می‌زد، و صفحه فقط با یک فرم خالی و بدون هیچ توضیحی
«رفرش» می‌شد — در حالی که در پس‌زمینه، خطای اعتبارسنجی به‌درستی رخ می‌داد اما به‌دلیل
همین باگ به کاربر نشان داده نمی‌شد.

### رفع شد

- **نصاب (`InstallController::installView`)**: مقادیر خطاهای هر فیلد (`error_for()`)
  و مقادیر قبلاً واردشده (`old()`) در صفحات نصب به‌درستی به موتور View اشتراک‌گذاری
  نمی‌شدند، بنابراین این دو تابع کمکی همیشه مقدار خالی برمی‌گرداندند و خطاها/مقادیر
  قبلی هرگز روی فرم‌های نصب نمایش داده نمی‌شدند.
- **پیام‌های فلش (`partials/flash.php`)**: این بخش با ساختار واقعی داده‌ای که
  `Session::pullFlash()` برمی‌گرداند مطابقت نداشت (انتظار آرایهٔ کلیددار بر اساس نوع
  پیام را داشت، در حالی که ساختار واقعی فهرستی از `{type, message}` است)، که باعث
  می‌شد پیام‌های موفقیت/خطا در تمام صفحات برنامه (نه فقط نصاب) یا اصلاً نمایش داده
  نشوند یا با آیکون و رنگ اشتباه نمایش داده شوند.

### روش تأیید

کل فرایند نصب از ابتدا تا انتها — شامل عمداً واردکردن رمز تکراری نادرست در مرحلهٔ
ساخت حساب مدیر — روی PHP 8.4 + MariaDB واقعی تکرار شد. اکنون در صورت وجود خطا،
پیام دقیق آن (مثلاً «تکرار «رمز عبور» مطابقت ندارد.») زیر همان فیلد نمایش داده
می‌شود و مقادیر درستِ واردشده در فیلدهای دیگر حفظ می‌مانند؛ پس از تصحیح داده‌ها،
نصب با موفقیت کامل شد (حساب مدیر ساخته شد، داده‌های نمونه درج شدند، نصاب به‌طور
خودکار غیرفعال شد، و ورود با حساب تازه‌ساخته‌شده کار کرد). خطای مشابه در صفحهٔ
ورود (رمز اشتباه) نیز اکنون به‌درستی نمایش داده می‌شود. مجموعه تست واحد پروژه
(۵۹ بررسی) و بررسی سرتاسری بیش از ۳۷ مسیر پنل مدیریت بدون خطا اجرا شدند.

## [0.1.2] — 1404/06/22 (2026-09-13)

رفع کامل خطای HTTP 500 گزارش‌شده پس از نصب روی هاست سی‌پنل، به‌همراه یک بررسی سرتاسری
تمام مسیرهای برنامه (پنل مدیریت، پرتال کارمند، پرتال مشتری، صفحات عمومی) روی محیط
واقعی PHP + MySQL برای یافتن و رفع خطاهای مشابه پیش از اینکه کاربران با آن مواجه شوند.

### رفع شد — خطاهای بحرانی که باعث خطای 500 در تمام صفحات می‌شدند

- **هسته روتر (`App.php`)**: میان‌افزارها (middleware) با نام کلاس اشتباه بارگذاری
  می‌شدند (مثلاً به‌جای `NotInstalledMiddleware` به دنبال `NotInstalled` می‌گشت) که
  باعث خطای «Class not found» در تمام درخواست‌ها می‌شد — همان خطایی که در لاگ سرور شما
  دیده شد.
- **هسته روتر (`App.php`)**: تشخیص کنترلر از روی رشتهٔ مسیر (`Admin\X@method`) به‌درستی
  انجام نمی‌شد و باعث ۴۰۴ شدن تمام مسیرهای پنل مدیریت می‌شد.
- **موتور View (`View.php`)**: محتوای اصلی هر صفحه (بخش `content`) در برخی حالت‌ها بازنویسی
  می‌شد و صفحات با بدنهٔ خالی نمایش داده می‌شدند.
- **schema.sql**: دو جفت نام محدودیت (constraint) تکراری بین جدول‌های `inventory`/`invoices`
  و `campaign_audiences`/`customer_addresses` باعث شکست کامل ساخت پایگاه‌داده در نصب
  می‌شد (خطای MySQL شمارهٔ 121).
- **نصاب (`InstallerService`)**: عدم تطابق تعداد ستون‌های آرایهٔ فاکتورهای نمایشی با
  حلقهٔ ثبت آن‌ها، که ثبت داده‌های نمونه را در میانهٔ کار متوقف می‌کرد.
- **محدودکنندهٔ نرخ درخواست (`RateLimitMiddleware`)**: پارامترهای `bucket,max,minutes`
  به‌درستی خوانده نمی‌شدند.
- **اعتبارسنج (`Validator`)**: قوانین `min`/`max` برای رشته‌هایی مثل شمارهٔ موبایل به‌اشتباه
  به‌صورت عددی بررسی می‌شدند (مثلاً موبایل ۰۹۱۲... به عدد ۹.۱۲ میلیاردی تبدیل و رد می‌شد).

### رفع شد — خطاهای صفحه‌به‌صفحه (یافته‌شده در بررسی کامل مسیرها)

- صفحهٔ «تنظیمات › شعبه‌ها»: ارجاع به ستون ناموجود `code` در جدول `branches`
  (نام درست ستون `slug` است) — هم در نمایش فهرست و هم در فرم افزودن/ویرایش شعبه.
- خروجی CSV مشتریان، نوبت‌ها، فاکتورها و گزارش‌ها: ناسازگاری با PHP 8.4 در تابع
  `fputcsv` که خروجی گرفتن را با خطا متوقف می‌کرد.
- خروجی CSV فاکتورها: نام ستون‌های اشتباه (`created_at`، `discount_total` و…)
  که با ستون‌های واقعی دیتابیس (`issue_date`، `discount_amount` و…) مطابقت نداشتند.
- «گردش انبار»: ارجاع به ستون ناموجود `user_id` در جدول تراکنش‌های انبار
  (نام درست `created_by` است).
- «گزارش عملکرد پرسنل»: استفادهٔ تکراری از یک پارامتر SQL هم‌نام در یک پرس‌وجو که
  با تنظیمات امنیتی PDO این پروژه (غیرفعال بودن emulated prepares) سازگار نبود.
- میز کار پرسنل (`/staff`) و صفحهٔ «عملکرد من» (`/staff/performance`): همین مشکل
  پارامتر تکراری، به‌علاوه ارجاع به ستون‌های ناموجود در جدول ارزیابی عملکرد.
- فاکتورهای مشتری در پرتال مشتری و پنل مدیریت، و چاپ فاکتور: ارجاع به نام ستون‌های
  نادرست برای وضعیت پرداخت، اقلام فاکتور و روش پرداخت.
- فرم ویرایش خدمت: خطای دسترسی به آرایهٔ خالی هنگام نمایش «دستور مصرف» خدمت.
- تب «فعالیت‌ها» در صفحهٔ جزئیات مشتری: نام ستون‌های اشتباه برای زمان و عنوان رویداد.
- ثبت فاکتور از صندوق فروش (POS): ترتیب اشتباه بررسی مقدار خالی که هنگام نبود نرخ
  مالیات سفارشی باعث خطا می‌شد.

### روش تأیید

تمام موارد بالا با اجرای واقعی پروژه روی PHP 8.4 + MariaDB (نه فقط بررسی نحوی کد)
و درخواست HTTP واقعی به بیش از ۶۰ مسیر پنل مدیریت، پرتال پرسنل، پرتال مشتری و صفحات
عمومی — شامل چند سناریوی ثبت/ویرایش (POST) — بازتولید و پس از رفع، دوباره آزمایش شدند.
مجموعه تست واحد پروژه (۵۹ بررسی) نیز بدون خطا اجرا شد.

## [0.1.1] — 1404/06/22 (2025-09-13)

هم‌راستاسازی طراحی رابط کاربری با طرح تأییدشده پورتال (design/maryamroshan.html) و
افزودن داده‌های نمایشی واقعی برگرفته از همان طرح.

### تغییر کرد

- پالت رنگ کامل رابط کاربری از رز-طلایی به **سرمه‌ای/آبی آسمانی** تغییر کرد
  (`--mr-primary:#384381`, `--mr-accent:#4690BF`) تا دقیقاً با طرح تأییدشده مطابقت داشته باشد؛
  توکن‌های `tokens.css` بازنویسی شدند و تمام اجزا (دکمه، نشان، جدول، KPI) به‌صورت خودکار
  رنگ جدید را گرفتند.
- نوار کناری پنل مدیریت از پس‌زمینه تیره به **پس‌زمینه روشن با حالت فعال آبی و خط لبه**
  تغییر کرد تا با طرح مطابقت داشته باشد؛ گروه‌بندی منو نیز با رنگ اختصاصی هر بخش
  (اصلی، فروش و مالی، بازاریابی، انبار، سیستم) بازطراحی شد.
- صفحه ورود/نصاب اکنون پس‌زمینه گرادیان متحرک با اشکال شناور مطابق طرح اولیه دارد.
- آیکون‌های نوار کناری از ایموجی به **Bootstrap Icons** (میزبانی محلی، بدون CDN) تغییر یافتند.

### افزوده شد

- سه شعبه (مرکزی، ونک، تجریش)، ۶ کارمند، ۸ مشتری، نوبت‌های امروز، ۵ فاکتور نمونه،
  تیکت و نظر — همگی با نام، شماره تماس، تاریخ تولد (تبدیل‌شده به میلادی) و ارقام برگرفته
  دقیقاً از طرح اولیه پورتال — در مرحله «داده‌های نمایشی» نصاب.
- تبدیل تقویم جلالی→میلادی داخلی و بدون وابستگی در `InstallerService` برای تاریخ تولد مشتریان.
- کتابخانه Bootstrap Icons به‌صورت محلی در `public/assets/vendor/bootstrap-icons/`.

## [0.1.0] — 1404/06/22 (2025-09-13)

نخستین انتشار کامل سامانه. تمام ماژول‌ها به‌صورت عمودی (پایگاه داده ← ریپازیتوری ←
سرویس ← کنترلر ← نما) پیاده‌سازی شده‌اند.

### افزوده شد

#### هسته و زیرساخت
- هسته MVC سبک اختصاصی: `Router`، `Request`، `Response`، `Database` (PDO)، `View`،
  `Session`، `Csrf`، `Env`، `Config`، `Logger` و مدیریت استثناها
- بارگذار خودکار PSR-4 بدون نیاز به Composer
- کنترلر جلویی `public/index.php` به‌همراه پیکربندی `mod_rewrite`
- نگاشت کامل مسیرها در `routes/web.php` و `routes/api.php`
- میان‌افزارها: `Auth`، `Guest`، `Csrf`، `Role`، `Permission`، `RateLimit`،
  `Installed`، `NotInstalled`
- کمک‌کننده `Jalali` برای تبدیل دوسویه تقویم شمسی/میلادی و `Format` برای اعداد و مبالغ فارسی

#### پایگاه داده
- طرح کامل `database/schema.sql` با **۱۱۴ جدول** در ماژول‌های احراز هویت، مشتریان،
  نوبت‌ها، خدمات، پرسنل، شعبه‌ها، مالی، انبار، بازاریابی، نظرات، تحلیل و سیستم
- تمام مبالغ با `DECIMAL(12,2)`؛ کلیدهای خارجی، ایندکس‌ها و حذف نرم (soft delete)
- داده‌های اولیه فارسی در `database/seeds.sql`: نقش‌ها، مجوزها، تنظیمات، سطوح وفاداری،
  دسته‌بندی و خدمات، قالب‌های پیامک و محتوای وب‌سایت

#### احراز هویت و دسترسی
- ورود با `password_hash` (bcrypt، cost=12)، نشست امن و بازتولید شناسه
- محافظت در برابر حمله جستجوی فراگیر با ثبت تلاش‌های ناموفق
- زیرساخت آماده برای ورود دومرحله‌ای (2FA/OTP)
- کنترل دسترسی مبتنی بر مجوز با ۶ نقش، اعمال‌شده در سمت سرور

#### نصاب
- نصاب چهارمرحله‌ای `/install`: خوش‌آمد، بررسی نیازمندی‌ها، پایگاه داده، حساب مدیر
- ساخت خودکار جداول و درج داده‌های پایه
- **خودغیرفعال‌سازی** پس از اتمام از طریق `storage/installed.lock`

#### پنل مدیریت
- داشبورد با شاخص‌های کلیدی، نمودار درآمد، نوبت‌های امروز و اقدامات سریع
- مدیریت مشتریان به‌همراه **پرونده ۳۶۰ درجه** (نوبت، فاکتور، پرداخت، خدمات، یادداشت،
  وفاداری، کیف پول، نظرات، خط زمانی فعالیت)
- مدیریت نوبت‌ها: فهرست، تقویم، ویزارد رزرو چندمرحله‌ای و جزئیات با تاریخچه وضعیت
- مدیریت خدمات، دسته‌بندی‌ها و **رسپی مصرف کالا**
- مدیریت کارکنان با ویرایشگر شیفت هفتگی، تخصص‌ها و پرونده عملکرد
- صندوق فروش (POS) با کاتالوگ، سبد، تخفیف، مالیات و تسویه
- فاکتورها: فهرست، جزئیات، ثبت پرداخت و نسخه چاپی
- انبار: سطح موجودی به تفکیک شعبه، مدیریت کالا، اصلاح دستی و دفتر گردش
- بازاریابی: کمپین پیامکی با بخش‌بندی، اتوماسیون «رویداد ← شرط ← اقدام» و کدهای تخفیف
- گزارش‌ها: فروش، نوبت، پرسنل، مشتری و انبار با بازه دلخواه، نمودار و خروجی CSV
- ورود اطلاعات از CSV با پیش‌نمایش و **گزارش خطای سطر‌به‌سطر**
- تنظیمات: پیکربندی عمومی، شعبه‌ها، کاربران و نقش‌ها، پشتیبان‌گیری و گزارش فعالیت‌ها
- مرکز اعلان‌ها با وضعیت خوانده‌نشده/خوانده‌شده/بایگانی

#### منطق کسب‌وکار
- موتور نوبت‌دهی با محاسبه بازه آزاد و **جلوگیری از رزرو همزمان در تراکنش پایگاه داده**
- موتور مالی: پرداخت چندروشی، کیف پول، تخفیف، مالیات و **استرداد به‌صورت تراکنش جبرانی**
- موتور پورسانت پیکربندی‌پذیر (درصدی/ثابت/پلکانی/بدون) با ذخیره محاسبات به تفکیک دوره
- ارزیابی عملکرد پرسنل با ۱۷ معیار
- باشگاه مشتریان با سطوح برنزی، نقره‌ای، طلایی و VIP (کاملاً قابل پیکربندی)
- مصرف خودکار موجودی انبار بر پایه رسپی خدمات
- لایه انتزاعی پیامک با درایورهای Kavenegar و Melipayamak و درایور `log` برای
  اجرای کامل سامانه **بدون نیاز به اعتبارنامه**

#### پرتال‌ها و وب‌سایت
- پرتال مشتری `/customer` با طراحی موبایل-اول
- پرتال پرسنل `/staff` شامل برنامه روز و هفته، پورسانت و عملکرد
- وب‌سایت عمومی سئو‌شده: صفحه اصلی، خدمات، تیم، گالری، وبلاگ، تماس و رزرو
- `sitemap.xml`، `robots.txt` و داده‌های ساخت‌یافته JSON-LD
- REST API داخلی `/api/v1/*` با پوشش پاسخ یکسان `{success, data}` / `{success, error}`

#### رابط کاربری
- توکن‌های طراحی `--mr-*` و لایه‌بندی: tokens → Bootstrap 5.3 RTL → app → utilities → skin
- مجموعه اجزای قابل استفاده مجدد: کارت، KPI، جدول، فرم، نشان وضعیت، مودال، تُست،
  خط زمانی، صفحه‌بندی، وضعیت خالی و اسکلت بارگذاری
- فونت Vazirmatn با میزبانی محلی؛ هیچ CDN و هیچ فرآیند بیلدی لازم نیست
- تمام اجزای پویا دارای چهار حالت **بارگذاری، موفقیت، خطا و خالی** هستند
- صفحات خطای اختصاصی ۴۰۳، ۴۰۴، ۴۱۹ و ۵۰۰

#### کران‌جاب‌ها
- `cron/daily.php`: تجمیع آمار روزانه، محاسبه RFM، علامت‌گذاری عدم مراجعه، اتوماسیون
  تولد و مشتریان غیرفعال، هشدار موجودی، پشتیبان‌گیری و پاک‌سازی
- `cron/reminders.php`: یادآوری ساعتی نوبت‌ها با تضمین عدم ارسال تکراری
- هر وظیفه ایزوله؛ شکست یکی مانع اجرای بقیه نمی‌شود

#### امنیت
- توکن CSRF روی تمام درخواست‌های تغییردهنده
- فرار خروجی سراسری در برابر XSS
- Prepared Statement در تمام کوئری‌ها
- محدودیت نرخ درخواست و اعتبارسنجی نوع MIME و حجم فایل‌های بارگذاری‌شده
- هدرهای امنیتی و غیرفعال‌سازی فهرست دایرکتوری
- گزارش ممیزی (Audit Log) برای عملیات حساس

#### تست و مستندات
- مجموعه تست سبک PHP با **۵۹ بررسی موفق** (`php tests/run.php`)
- `README.md` کامل فارسی شامل نصب، ساختار، ماژول‌ها و راهنمای استقرار
- `.env.example` با تمام کلیدهای پیکربندی؛ `.env` هرگز کامیت نمی‌شود

### یادداشت‌های مهاجرت

نخستین انتشار؛ مهاجرتی لازم نیست.

[0.1.1]: https://github.com/shimmercode/MARYAM-ROSHAN/compare/v0.1.0...v0.1.1
[0.1.0]: https://github.com/shimmercode/MARYAM-ROSHAN/releases/tag/v0.1.0
